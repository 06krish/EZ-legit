# Portfolio Krish

This project runs as a Next.js portfolio site.

## Scripts

- `npm run dev` starts the local development server.
- `npm run build` creates a production build.
- `npm run start` serves the production build.
- `npm run lint` runs ESLint.

## Main App Entry

The homepage renders from `app/page.jsx` and uses `components/portfolio-page.jsx`.
